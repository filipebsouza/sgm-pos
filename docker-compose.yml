version: '3'
services: 
    app:
        build:
            context: .
            dockerfile: ./src/Dockerfile
        ports: 
            - '80:3000'
        # Para funcionamento no Windows
        tty: true